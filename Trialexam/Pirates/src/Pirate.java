import java.util.ArrayList;
import java.util.List;

class Pirate {
    String name;
    boolean hasWoodenLeg;
    int gold;

    Pirate(String name, boolean hasWoodenLeg, int gold) {
        this.name = name;
        this.hasWoodenLeg = hasWoodenLeg;
        this.gold = gold;
    }

    public static void listModofied(List<Integer> pirates) {
        List<Integer> listModified = new ArrayList<>();
        for (hasWoodenLegs && gold > 15) {
            pirates.add(name)
        }
        return listModified;
    }
}